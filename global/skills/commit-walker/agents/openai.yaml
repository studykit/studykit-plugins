interface:
  display_name: "commit-walker"
  short_description: "Walk through git history commit by commit"
  default_prompt: "Use the commit-walker skill only when explicitly requested."

policy:
  allow_implicit_invocation: false
