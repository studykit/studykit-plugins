interface:
  display_name: "commit"
  short_description: "Commit staged git changes"
  default_prompt: "Use the commit skill only when explicitly requested."

policy:
  allow_implicit_invocation: false
