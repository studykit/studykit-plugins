interface:
  display_name: "rollout"
  short_description: "Write a resumable session handoff"
  default_prompt: "Use the rollout skill only when explicitly requested."

policy:
  allow_implicit_invocation: false
