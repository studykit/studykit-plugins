interface:
  display_name: "get-api-docs"
  short_description: "Fetch current third-party API documentation"
  default_prompt: "Use the get-api-docs skill only when explicitly requested."

policy:
  allow_implicit_invocation: false
