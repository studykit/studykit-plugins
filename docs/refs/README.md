# Collected documentation references

Local copies of official documentation cited as evidence in this repository,
saved by the guard plugin's Grounded output style (`refs_dir` in
`.claude/guard.local.json` points here). Each file records the relevant excerpt
of one source and names its origin URL, so a citation stays inspectable after
the upstream page changes. Files are committed through the normal git workflow.
