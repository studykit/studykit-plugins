interface:
  display_name: "r"
  short_description: "Revise English text in technical style"
  default_prompt: "Use the r skill only when explicitly requested."

policy:
  allow_implicit_invocation: false
