# ArrangeResultEnvelopes.objectType Property

Parent Object: [ArrangeResultEnvelopes](ArrangeResultEnvelopes.htm)
Defined in namespace "adsk::fusion" and the header file is <Fusion/Arrange/ArrangeResultEnvelopes.h>

## Description

This property is supported by all objects in the API and returns a string that contains the full name (namespace::objecttype) describing the type of the object.

## Syntax

* [Python](#Python)
* [C++](#C++)

"arrangeResultEnvelopes\_var" is a variable referencing an ArrangeResultEnvelopes object.  ```` ``` # Get the value of the property. propertyValue = arrangeResultEnvelopes_var.objectType ``` ```` |

"arrangeResultEnvelopes\_var" is a variable referencing an ArrangeResultEnvelopes object. ```` ``` #include <Fusion/Arrange/ArrangeResultEnvelopes.h>  // Get the value of the property. string propertyValue = arrangeResultEnvelopes_var->objectType(); ``` ```` |

## Property Value

This is a read only property whose value is a string.

## Version

Introduced in version January 2025

---

|  |  |
| --- | --- |
| © Copyright 2025 Autodesk, Inc. | Comment on this page. |