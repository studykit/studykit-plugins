interface:
  display_name: "Audit a turn"
  short_description: "Audit a response with your configured reviewer"
  default_prompt: "Use $audit-turn to review the last completed response with the configured reviewer."
policy:
  allow_implicit_invocation: false
