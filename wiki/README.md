# studykit-plugins Knowledge Wiki

This directory is the repository-backed knowledge root for `studykit-plugins`.

Knowledge pages are organized by plugin.

## Plugins

- [Workflow](workflow/README.md)

## Policy

Use plugin-specific subdirectories instead of placing plugin pages directly under `wiki/`.

This keeps knowledge pages discoverable as the repository grows across multiple plugins.
