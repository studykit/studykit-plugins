# shellbox

This is a Herdr-only plugin. Keep its shell sessions independent of Lens and
never send shell input to the source pane. See [dev/testing.md](dev/testing.md)
for host verification.

Run `python3 -m unittest discover -s shellbox/tests` from the repository root.
