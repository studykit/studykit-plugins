The configured issue provider is `filesystem`. There is no writeback
script — edit the issue's markdown file at the path the authoring
resolver returns and commit the change through normal version control.
