References are Jira issue keys (e.g., `PROJ-123`) or text containing them.
Cached `comments` paths point to sibling `comment-*.md` files — read each
for one cached comment. `issue.md` frontmatter carries the issue title,
state, labels, `remote_links`, and `relationships`. Never edit
`comment-*.md` files in place.
