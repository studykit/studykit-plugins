Jira references use issue keys (e.g., `PROJ-123`). The Jira backend also
accepts the Epic-Link family `--epic <KEY>`, `--replace-epic <KEY>`, and
`--remove-epic` on top of the standard relationship flags.
