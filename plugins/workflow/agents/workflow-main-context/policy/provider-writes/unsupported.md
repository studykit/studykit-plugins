## unsupported issue provider

The configured issue provider is not supported by workflow scripts. Do not
guess a `github_issue_*.py` or `jira_issue_*.py` command for this project.
Report the limitation to the user instead of attempting a provider write.
