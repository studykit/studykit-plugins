<bash>
spectrack mustread --type <type> [flags]
</bash>

Before drafting any workflow issue or knowledge artifact, always run
`spectrack mustread --help` first — it documents the flags and the authoring
policy. Then run the resolver: its output lists the files to read before
drafting and a notes section whose every bullet is a binding rule for this
flow.
