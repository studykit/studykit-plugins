interface:
  display_name: "Workflow Setup"
  short_description: "Initialize .spectrack/config.yml for workflow providers"
  default_prompt: "Set up workflow for this repository"

policy:
  allow_implicit_invocation: false
