interface:
  display_name: "handover"
  short_description: "Write a resumable session handover"
  default_prompt: "Use the handover skill only when explicitly requested."

policy:
  allow_implicit_invocation: false
