{
  "name": "dev-rules",
  "version": "0.4.0",
  "description": "Gate file edits via a PreToolUse deny + reason that points to the project's coding rule sets (e.g. logging conventions) and tells the model to Read them before retrying. Per-language dedup ensures each ruleset is announced at most once per session.",
  "author": {
    "name": "studykit"
  },
  "repository": "https://github.com/studykit/studykit-plugins",
  "license": "MIT",
  "keywords": ["dev-rules", "logging", "hooks", "coding-rules", "context-injection"]
}
