# Handover contributor notes

Keep runtime assumptions and validation evidence in [runtime notes](dev/runtime.md).

Run `python3 -m unittest discover -s handover/tests` and `claude plugin validate handover` from the marketplace root after changes.
